

Общее время затраченное на работу с учетом разбора кода - 10..12часов


Проблемы с адаптивом и неработающие элементы:

- не работает значек, предназначенный для перемотки страницы при нажатии на него

- адаптив: описание врача не вмещается в выведенное для этого поле - текст слишком 
           большой (выступает за края) (ИСПРАВЛЕНО)
-адаптив: при изменении ширины экрана кнопка, предназначенная для скрола страницы не зафиксированак основному блоку страницы 
          с информацией (ИСПРАВЛЕНО)
-адаптив: не обязательно делать несколько видов и вариантов расширения экрана и прописывать такой большой код для каждого из классов 
          и ID. Достаточно прописать основные разрешения для экранов таким образом, например: 
          @media screen and (min-width:576px) and (max-width:900px) {...}
          таким образом можно существенно сократить написанный код в стилях (ПРЕДСТАВЛЕНО НИЖЕ, КАК ПРИМЕР, В ВЕРСТКЕ ПРИ РАСШИРЕНИИ 
          ЭКРАНА МЕНЕЕ 600рх)
-адаптив: в шапке логотип при расширении экрана менее 801рх накладывается на имя консультанта (ИСПРАВЛЕНО)
-адаптив: при расширении экрана менее 991рх логотип немного уменьшаем, поскольку он неравномерно смотрится в шапке сайта (ИСПРАВЛЕНО)
-адаптив: при расширении экрана менее 600рх вообще не работает адаптив. Поскольку для телефонов - это расширение где-то до 320рх, 
          сделаем адаптив с условием минимум 320рх, максимум 600рх(ИСПРАВЛЕНО)



Для отправки значений по экшену формы после нажатия кнопки "отправить" необходимо в файле .php указать следущее:

(как пример)

<?php

   // поля из формы

   $name = $_POST['name'];
   $phone = $_POST['phone'];
   $answers = $_POST['answers'];

   // токен

   $arr = array(
     'Моб.телефон: ' => $phone,
     'Имя пользователя: ' => $name,
     'Ответы пользователя (пол, год рождения и наличие аллергии): ' => $answers
   );

   foreach($arr as $key => $value) {
     $txt .= "<b>".$key."</b> ".$value."%0A";
   };


?>
